apply plugin: 'groovy'

dependencies {
    compile gradleApi()
    compile localGroovy()
    testCompile "junit:junit:${project.ext.junitVersion}"
}

compileGroovy {
    sourceCompatibility = 1.8
    targetCompatibility = 1.8
}

defaultTasks 'build'